# ChallanAI — Web UI

Drag the `challanai_web/` folder into [vercel.com/new](https://vercel.com/new) to deploy instantly.

Set the environment variable `BASE_URL` to your Cloud Run API URL (e.g. `https://challanai-xxxxx-el.a.run.app`).
